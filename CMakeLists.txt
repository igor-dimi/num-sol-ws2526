# CMakeLists.txt at repository root (forwarder)
cmake_minimum_required(VERSION 3.16)
# You can declare the project here or let src/ do it; either is fine.
# project(numerics_solutions CXX)

add_subdirectory(src)
